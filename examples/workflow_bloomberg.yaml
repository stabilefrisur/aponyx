# Bloomberg data workflow configuration
# Usage: aponyx run --config workflow_bloomberg.yaml
# Note: Requires Bloomberg terminal connection and xbbg package

signal: spread_momentum
strategy: balanced
data: bloomberg

# Run all steps
force: false
