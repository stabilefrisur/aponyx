# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
venv/
env/

# Data files (runtime-generated, not version controlled)
data/raw/
data/processed/
data/cache/
data/registry.json

# Logs and runtime metadata (not version controlled)
*.log
logs/

# Reports (evaluation outputs - not version controlled)
reports/suitability/
reports/performance/

# Example outputs (demo artifacts - not version controlled)
src/aponyx/examples/data/
src/aponyx/examples/reports/

# Suitability registry (data-dependent, not version controlled)
src/aponyx/evaluation/suitability/suitability_registry.json

# Performance registry (data-dependent, not version controlled)
src/aponyx/evaluation/performance/performance_registry.json

# Static JSON configuration files (INCLUDE in version control):
# These are version controlled via negation patterns below
!src/aponyx/models/signal_catalog.json
!src/aponyx/backtest/strategy_catalog.json
!src/aponyx/data/bloomberg_securities.json
!src/aponyx/data/bloomberg_instruments.json

# Test and coverage artifacts
.pytest_cache/
.coverage
htmlcov/
.tox/
coverage.xml
*.cover

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
.VSCodeCounter/

# Jupyter Notebooks
.ipynb_checkpoints/
# Ignore notebooks in general, but allow examples and workflow notebooks
*.ipynb
!src/aponyx/examples/**/*.ipynb
!src/aponyx/notebooks/**/*.ipynb

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Ruff
.ruff_cache/

# Environment variables
.env
.env.local

# Release checklists (internal use only)
PYPI_RELEASE_CHECKLIST.md
PYPI_RELEASE_AGENT_INSTRUCTIONS.md

# Internal TODO tracking (not version controlled)
TODO.md

# Utility scripts (not version controlled)
scripts/
